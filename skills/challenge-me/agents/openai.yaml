interface:
  display_name: "Challenge Me"
  short_description: "Question a proposal until its decisions are clear"
  default_prompt: "Use $challenge-me to pressure-test this proposal and expose unresolved decisions."
policy:
  allow_implicit_invocation: false
